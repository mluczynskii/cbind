{#- luareg.c.j2
  The luaL_Reg structure binds wrapper names to their 
  implementation in the binding code. It basically functions
  as a Lua module description, and needs to be defined above the 
  function generated by luainit.c.j2. This template works both
  for the main register and registers for data-structures.

  name - Name of the register
  functions - An array of function names that are part of the module
-#}
const luaL_Reg {{ register["register_name"] }}[] = {
  {%- for function in register["functions"] %}
  {"{{ function["lua_name"] }}", {{ function["c_name"] }}},
  {%- endfor %}
  {NULL, NULL}
};